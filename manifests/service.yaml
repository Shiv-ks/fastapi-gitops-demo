apiVersion: v1
kind: Service
metadata:
  name: fastapi-gitops-demo-service
spec:
  selector:
    app: fastapi-gitops-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer

